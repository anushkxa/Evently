When our Spring Boot app itself runs inside a container, localhost refers to that container's own network namespace not our host machine, and not the Postgres container. Each container gets its own isolated network stack by default. So when your app tries to connect to localhost:5432, it's looking for Postgres running inside its own container, finds nothing listening there, and the connection fails (usually Connection refused).